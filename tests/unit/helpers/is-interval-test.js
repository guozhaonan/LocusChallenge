import { isInterval } from 'locus-challenge/helpers/is-interval';
import { module, test } from 'qunit';

module('Unit | Helper | is interval');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isInterval([42]);
  assert.ok(result);
});
