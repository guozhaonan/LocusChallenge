import { monthsOptions } from 'locus-challenge/helpers/months-options';
import { module, test } from 'qunit';

module('Unit | Helper | months options');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = monthsOptions([42]);
  assert.ok(result);
});
