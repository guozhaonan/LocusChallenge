import { yearsOptionTwo } from 'locus-challenge/helpers/years-option-two';
import { module, test } from 'qunit';

module('Unit | Helper | years option two');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = yearsOptionTwo([42]);
  assert.ok(result);
});
