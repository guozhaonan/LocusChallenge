import { weeksOptions } from 'locus-challenge/helpers/weeks-options';
import { module, test } from 'qunit';

module('Unit | Helper | weeks options');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weeksOptions([42]);
  assert.ok(result);
});
