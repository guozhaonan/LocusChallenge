import { daysOptions } from 'locus-challenge/helpers/days-options';
import { module, test } from 'qunit';

module('Unit | Helper | days options');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = daysOptions([42]);
  assert.ok(result);
});
