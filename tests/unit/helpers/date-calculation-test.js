import { dateCalculation } from 'locus-challenge/helpers/date-calculation';
import { module, test } from 'qunit';

module('Unit | Helper | date calculation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateCalculation([42]);
  assert.ok(result);
});
