import { errorOptions } from 'locus-challenge/helpers/error-options';
import { module, test } from 'qunit';

module('Unit | Helper | error options');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = errorOptions([42]);
  assert.ok(result);
});
