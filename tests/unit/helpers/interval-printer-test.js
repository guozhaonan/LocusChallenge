import { intervalPrinter } from 'locus-challenge/helpers/interval-printer';
import { module, test } from 'qunit';

module('Unit | Helper | interval printer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = intervalPrinter([42]);
  assert.ok(result);
});
