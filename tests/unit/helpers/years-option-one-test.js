import { yearsOptionOne } from 'locus-challenge/helpers/years-option-one';
import { module, test } from 'qunit';

module('Unit | Helper | years option one');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = yearsOptionOne([42]);
  assert.ok(result);
});
